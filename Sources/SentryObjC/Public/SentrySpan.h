// SentryObjC compatibility — the protocol lives in SentrySpanProtocol.h
// (re-exported from the main SDK). This header exists so consumers that
// `#import "SentrySpan.h"` continue to compile.
#import "SentrySpanProtocol.h"
