name: Test Cross-Platform Sentry SDK Installation

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - '.github/workflows/test-cross-platform.yml'
      - 'Sources/**'
      - 'Sentry.podspec'

jobs:
  # inspired by https://github.com/getsentry/sentry-react-native/blob/main/.github/workflows/native-tests.yml
  test-react-native:
    name: React Native
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          repository: getsentry/sentry-cocoa
          path: sentry-cocoa

      - name: Clone Sentry React Native Repository
        uses: actions/checkout@v4
        with:
          repository: getsentry/sentry-react-native
          path: sentry-react-native

      - name: Enable Corepack
        working-directory: sentry-react-native
        run: npm i -g corepack

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'yarn'
          cache-dependency-path: sentry-react-native/yarn.lock

      - name: Install SDK JS Dependencies
        working-directory: sentry-react-native
        run: yarn install

      # the next two steps inspired by https://github.com/getsentry/sentry-react-native/blob/main/.github/workflows/native-tests.yml
      - name: Remove Sentry/HybridSDK Dependency from RNSentry.podspec
        run: sed -i '' "/s.dependency 'Sentry\/HybridSDK',/d" sentry-react-native/packages/core/RNSentry.podspec

      - name: Add Sentry/HybridSDK Dependency to RNSentryCocoaTester/Podfile
        run: sed -i '' "s/RNSentry.podspec'/RNSentry.podspec'\n  pod 'Sentry\/HybridSDK', :path => '../sentry-cocoa'\nend/" sentry-react-native/packages/core/RNSentryCocoaTester/Podfile

      - name: Install App Pods
        working-directory: sentry-react-native/packages/core/RNSentryCocoaTester
        run: pod install

      - name: Run iOS Tests
        working-directory: sentry-react-native/packages/core/RNSentryCocoaTester
        env:
          SCHEME: RNSentryCocoaTester
          CONFIGURATION: Release
          DESTINATION: 'platform=iOS Simulator,OS=latest,name=iPhone 16'
        run: |
          env NSUnbufferedIO=YES \
            xcodebuild -workspace *.xcworkspace \
            -scheme $SCHEME -configuration $CONFIGURATION \
            -destination "$DESTINATION" \
            test
