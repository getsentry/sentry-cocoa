#include "../Shared/Config/_Common.xcconfig"

PRODUCT_BUNDLE_IDENTIFIER = io.sentry.iOS15-SwiftUI
INFOPLIST_FILE = iOS15-SwiftUI/Info.plist
CODE_SIGN_ENTITLEMENTS = iOS15-SwiftUI/iOS15-SwiftUI.entitlements;

SUPPORTED_PLATFORMS = iphoneos iphonesimulator

IPHONEOS_DEPLOYMENT_TARGET = 15.0

DEVELOPMENT_ASSET_PATHS = "iOS15-SwiftUI/Preview Content"
